classdef Ops < matlab.unittest.TestCase
  methods (Test)
    % Ops
%     randomdataset(test)
    identityn(test)
    
    % MathOps
    add(test)
    mul(test)
    bucketize(test)
    matrixsolve(test)
  end
end
